from setuptools import setup, find_packages

setup(
    name="drone_sound_separator",
    version="0.1",
    packages=find_packages(),
    description="A tool for separating drone sounds from audio",
    author="Your Name",
    author_email="your.email@example.com",
    install_requires=[
        # List your dependencies here
    ],
)
